# EXPO_PUBLIC_API_URL is no longer required.
# The app runs fully offline using local SQLite (expo-sqlite).
# This file is kept for reference in case a server sync feature is added later.
#
# EXPO_PUBLIC_API_URL=http://localhost:3000
